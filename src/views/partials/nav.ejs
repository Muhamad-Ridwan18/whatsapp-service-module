<nav class="border-b border-surface-200 bg-surface-50/90 backdrop-blur sticky top-0 z-50">
  <div class="max-w-5xl mx-auto px-4 py-3 flex items-center justify-between gap-4">
    <a href="/dashboard" class="flex items-center gap-2.5 shrink-0">
      <span class="w-9 h-9 rounded-xl bg-accent/20 text-accent flex items-center justify-center text-sm font-bold">WA</span>
      <div class="hidden sm:block">
        <p class="font-semibold text-white text-sm leading-tight">WhatsApp Service</p>
        <p class="text-[10px] text-gray-500">Panel Admin</p>
      </div>
    </a>
    <div class="flex items-center gap-1 sm:gap-2 text-sm">
      <a href="/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'nav-link-active' : '' %>">Beranda</a>
      <% if (typeof currentUser !== 'undefined' && currentUser && currentUser.role === 'super_admin') { %>
        <a href="/dashboard/users" class="nav-link <%= activePage === 'users' ? 'nav-link-active' : '' %>">Pengguna</a>
      <% } %>
      <a href="/dashboard/settings" class="nav-link <%= activePage === 'settings' ? 'nav-link-active' : '' %>">Profil</a>
      <a href="/docs" class="nav-link hidden md:inline">Dokumentasi</a>
      <a href="/logout" class="btn-secondary text-xs py-1.5 px-3 ml-1">Keluar</a>
    </div>
  </div>
</nav>
